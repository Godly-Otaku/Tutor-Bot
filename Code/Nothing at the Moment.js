const Discord = require("discord.js");
const { prefix, darkgreen } = require("../config.json");

module.exports = {
    name: 'Nothing at the Moment',
    description: ' ',
    cooldown: 5,
    class: 'fun',
    args: false,
    execute(msg, args, client, con, catchErr) {

    },
};